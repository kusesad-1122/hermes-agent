{
  "name": "@hermes/android",
  "private": true,
  "version": "0.1.0",
  "description": "Hermes Agent — Android app (Capacitor WebView shell around the web dashboard).",
  "type": "module",
  "scripts": {
    "_comment": "Build the dashboard first (npm --workspace web run build), then `add:android` once, then `sync` after each rebuild, then `open` to build the APK in Android Studio.",
    "add:android": "cap add android",
    "sync": "cap sync android",
    "open": "cap open android",
    "run": "cap run android",
    "build:apk": "cd android && ./gradlew assembleDebug",
    "doctor": "cap doctor"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
